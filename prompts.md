# Code Refactoring Prompts

- Audit this module for duplication across functions, tests, and adjacent modules. Build a small inventory of repeated logic, compare the call sites, identify which pieces truly share behavior, then refactor only the parts that can be unified without changing contracts, error behavior, or public API. After each change, re-check the surrounding code for simpler follow-up cleanups that became obvious.
- Analyze this repository for real bugs and fix them with focused, behavior-preserving changes. Inspect error handling, boundary conditions, invalid inputs, state transitions, async behavior, persistence logic, serialization contracts, tests, and assumptions around optional or missing data. Prioritize defects that can cause incorrect behavior, data loss, panics, security issues, or broken public contracts, write or update regression tests for each confirmed bug, and avoid broad refactors that are not required to fix the issue.
- Refactor this code for long-term maintainability while preserving every observable behavior. First map its inputs, outputs, side effects, error cases, and edge cases, then split mixed responsibilities into clearer local pieces. Prefer clearer names, narrower scopes, and direct expressions over new abstractions unless the code remains difficult to reason about after the basic cleanup.
- Review architecture boundaries across crates and modules to ensure responsibilities are not mixed and hidden coupling has not accumulated. Map each crate, module, route, service, domain type, adapter, and shared helper to its intended responsibility, then identify dependencies that point in the wrong direction, duplicated domain logic, leaking implementation details, and shared utilities that obscure ownership. Move logic only when it clearly belongs at another boundary, keep public APIs minimal, preserve behavior, and verify that tests still exercise the same external contracts.
- Audit the public API surface of this crate and minimize it without changing intended external behavior. Find unnecessary `pub`, exposed struct fields, re-exports, public helper types, public constructors, and implementation details that accidentally became part of the API. Reduce visibility to `pub(crate)` or private where possible, keep only the externally justified API public, and add or update focused tests for public behavior that must remain stable.
- Audit dependency hygiene across all `Cargo.toml` files in this workspace. Check that crates.io dependencies are declared only in `[workspace.dependencies]`, workspace crates use `dependency.workspace = true`, default features are disabled unless explicitly required, versions are not duplicated or drifted, and no unused or unnecessary dependencies remain. Preserve existing behavior, avoid adding new crates unless explicitly requested, and verify the result with the appropriate Cargo checks.
- Clean up naming across this codebase so identifiers communicate domain meaning without abbreviations or hidden context. Audit variables, functions, methods, traits, constants, structs, enums, modules, type aliases, fields, parameters, database schema names, and test helpers for shortened, vague, overloaded, or implementation-focused names. Rename them to explicit full-word domain names, preserve external contract string values, avoid casual public API renames unless explicitly justified, and verify that the refactor is purely mechanical where behavior must stay unchanged.
- Audit repeated string literals and replace them with existing shared constants wherever possible. Search handlers, services, domain modules, tests, fixtures, error handling, validation, API contracts, SQL construction, and configuration adapters for duplicated literal values or reusable message fragments. Reuse existing constants before introducing new ones, add new shared constants only when no suitable constant exists, preserve external contract values exactly, and verify that the change is a mechanical reuse cleanup rather than a behavior change.
- Audit SQL query construction for hardcoded schema identifiers and replace them with shared constants. Search handlers, models, repositories, migrations-adjacent helpers, idempotency code, auth code, tests, and fixtures for inline table names, column names, field names, and schema fragments. Reuse existing `table_names::*`, `COLUMN_*`, `FIELD_*`, and `TABLE_*` constants where available, introduce new shared constants only when no suitable constant exists, and rebuild SQL with `format!` while preserving the exact generated query behavior.
- Audit the Swagger or OpenAPI schema against the actual API implementation and synchronize any drift. Compare documented paths, methods, request bodies, query parameters, headers, response status codes, response schemas, error formats, authentication requirements, pagination rules, and examples with the route handlers, client helpers, domain types, and tests. Fix whichever side is wrong, preserve intentional external contracts, add or update contract and round-trip tests where useful, and verify that generated documentation and runtime API behavior stay aligned.
- Search for implicit domain logic represented by strings, loosely typed values, magic constants, or repeated condition checks. For each case, decide whether a domain-specific enum or small type would make call sites safer and clearer. Preserve external protocol values and public contracts, and only introduce new types where they remove ambiguity rather than adding ceremony.
- Replace explicit primitive types with domain-specific types throughout this codebase wherever primitives carry business meaning. Audit `String`, `&str`, integers, booleans, UUIDs, IDs, counters, limits, statuses, permissions, and other raw values at module, service, route, and persistence boundaries. Introduce narrow newtypes or enums that encode invariants and intent, require initialization through `From` or `TryFrom` implementations with explicit validation and bounds checks, preserve external contracts through explicit conversions, and keep plain primitives only where they are purely technical implementation details with no domain meaning.
- Audit numeric and type conversions for safety and replace risky casts with explicit `From` or `TryFrom` conversions. Search for `as` casts, lossy integer conversions, sign changes, truncation, widening that hides intent, unchecked enum discriminant conversions, and conversions near serialization, database, API, and domain boundaries. Add explicit bounds checks where needed, propagate conversion errors with domain-specific error types, preserve external values, and verify edge cases with focused tests.
- Audit tests for determinism and remove flaky assumptions. Search for wall-clock time, timezone dependence, `sleep`, real external services, network calls, random data without fixed seeds, order-dependent assertions, shared mutable state, and timing-sensitive async behavior. Replace them with injected time, deterministic fixtures, local fakes, stable ordering, explicit seeds, and focused helpers, then verify that the tests remain meaningful and can run reliably in isolation and in parallel.
- Refactor test code to reuse shared setup, fixtures, builders, assertions, and route-call helpers where duplication makes tests harder to maintain. First inventory repeated test arrangements and expected outcomes, then extract only the helpers that clarify intent across multiple tests. Keep single-use logic inline, preserve deterministic behavior, avoid hiding important assertions behind opaque helpers, and ensure route tests continue to reuse the corresponding `call_*_route_client` functions.
- Refactor repetitive tests with macros where the repetition hides intent or makes coverage hard to maintain. First inventory duplicated setup, assertions, route calls, input matrices, and contract or round-trip cases, then decide whether a local `macro_rules!` macro is enough or whether a dedicated proc-macro crate would materially improve clarity. Prefer the simplest macro that keeps test failures readable, avoid adding proc-macro dependencies unless explicitly justified, and verify that generated tests stay deterministic and preserve existing route-client and helper usage patterns.
- Replace custom `macro_rules!` macros with a dedicated proc-macro crate where declarative macros have become hard to read, validate, compose, or debug. First inventory every macro, its call sites, generated code shape, hygiene assumptions, error messages, and compile-time behavior, then migrate only the macros that benefit from typed parsing and clearer diagnostics. Keep the proc-macro crate minimal, use workspace dependency rules, preserve generated public API and test names where possible, and verify the migration with focused compile, test, and failure-diagnostic checks.
- Audit async correctness across this workspace. Check that the code uses a single async runtime consistently, does not block executor threads, does not hold locks across `.await`, does not spawn tasks without explicit error ownership and cancellation policy, and does not hide cancellation-sensitive side effects behind helper abstractions. Refactor risky code in small behavior-preserving steps, keep async boundaries explicit, and verify the result with focused tests or documented reasoning where concurrency behavior is difficult to test deterministically.
- Remove unnecessary lint `allow` attributes from this workspace. Audit `#[allow(...)]`, `#![allow(...)]`, conditional allow attributes, and test-only lint suppressions, then determine whether each suppression still hides a real issue or can be removed by improving the code. Prefer fixing the underlying warning, preserve only justified suppressions that are explicitly required, avoid adding new suppressions, and verify the cleanup with `cargo clippy --all-targets --all-features -- -D warnings`.
- Inspect ownership, borrowing, and allocation patterns throughout this code path. Find unnecessary cloning, temporary collections, string allocations, and avoidable conversions, then replace them with borrowing or iterator-based flows where that keeps the code readable. Pay special attention to hot paths and large structures, and verify that each change keeps lifetimes understandable.
- Remove unnecessary memory allocations from this code path without changing behavior. Trace where heap allocations happen, including `String` creation, `Vec` growth, `clone()` calls, intermediate collections, formatting, boxing, and conversions between owned and borrowed values. Replace them with borrowing, pre-sized collections, streaming iteration, or direct references where appropriate, and verify that each optimization keeps the code simple, deterministic, and covered by tests or clear reasoning.
- Refactor inefficient code paths with better algorithms while preserving observable behavior and public contracts. Identify nested scans, repeated sorting, repeated lookups, avoidable full collection traversal, unnecessary intermediate data structures, and operations whose complexity grows poorly with input size. Replace them with simpler or more efficient algorithms only when the improvement is justified, keep memory usage bounded, document non-obvious complexity tradeoffs, and verify correctness with focused edge-case tests plus benchmarks or explicit complexity analysis.
- Optimize iterator pipelines that create unnecessary adapters, intermediate collections, repeated passes, or hidden allocations. Inspect chained `map`, `filter`, `collect`, `flat_map`, `cloned`, `copied`, `filter_map`, sorting, grouping, and lookup patterns to determine whether the same result can be produced with a simpler single-pass iterator flow. Preserve readability and borrowing clarity, avoid manual loops unless they are materially clearer or faster, and verify behavior with focused tests plus benchmarks or explicit complexity reasoning for hot paths.
- Audit cache locality and data layout in performance-sensitive code without changing the public API. Inspect struct and enum sizes, field ordering, large variants, pointer chasing through boxed or nested data, scattered related collections, repeated indirection, and access patterns that fight CPU cache behavior. Improve locality with layout adjustments, tighter data grouping, contiguous storage, or reduced indirection only where measurements or clear access patterns justify the change, and verify that serialization, persistence, and external contracts remain unchanged.
- Analyze the performance-sensitive parts of this code before changing them. Identify loops, repeated lookups, avoidable allocations, blocking calls, lock usage, and conversions inside hot paths. Apply small refactors that reduce overhead without changing behavior, then verify the result with focused tests, benchmarks, or explicit reasoning when benchmarks are not available.
- Bring this repository into alignment with the applicable `AGENTS.md` instructions. First read every relevant `AGENTS.md` file, map the rules to the current workspace structure, then audit code, tests, dependency declarations, error types, naming, visibility, SQL constants, and verification commands for violations. Fix issues in small behavior-preserving steps, avoid unrelated cleanup, and keep iterating until the repository satisfies the instructions or any remaining gaps are explicitly documented with reasons.
