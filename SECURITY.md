# Security policy

Report vulnerabilities privately to the repository owner. Do not include credentials, production
data, exploit payloads against public systems, or sensitive logs in a public issue.

Supported releases are the current default branch and explicitly published release tags. Secrets
belong in the deployment secret store. Rotate any value accidentally committed or exposed in logs;
removing it from Git is not sufficient.
